# 用户资料保存遮罩层不关闭 — 修复指南

## 问题描述

用户在"个人中心"页面编辑昵称或头像链接后，点击"保存修改"按钮，全局加载遮罩层（`#global-loading`）出现后不会自动关闭，导致页面被遮挡无法操作。

## 根因分析

### 调用链路

1. 用户点击"保存修改" → 触发 `saveProfile()` 函数
2. `saveProfile()` 内部调用 `$.ajax()` 发起 `PUT /api/auth/profile` 请求
3. 请求发送前，`$.ajaxSetup` 中的全局 `beforeSend` 回调调用 `showLoading()` → 遮罩层出现 ✅
4. 请求完成后，局部 `complete` 回调仅恢复了按钮状态 → 遮罩层未关闭 ❌

### 核心原因：jQuery 回调覆盖机制

在 `common.js` 中，`$.ajaxSetup()` 配置了全局 `complete` 回调：

```javascript
// common.js - 全局配置
$.ajaxSetup({
    beforeSend: function (xhr, settings) {
        if (!settings.skipGlobalLoading) {
            showLoading();  // 显示遮罩层
        }
        // ...
    },
    complete: function (_, statusText) {
        hideLoading();  // 隐藏遮罩层 ← 关键逻辑
        // ...
    },
    error: function (xhr) {
        hideLoading();  // 错误时也隐藏遮罩层
        // ...
    }
});
```

在 `app.js` 的 `saveProfile()` 中，`$.ajax()` 提供了局部 `complete` 回调：

```javascript
// app.js - 修复前（有 Bug）
$.ajax({
    // ...
    complete: function () {
        // 仅恢复按钮状态，未调用 hideLoading()
        $('#profile-save-btn').prop('disabled', false).text('保存修改');
    }
});
```

**jQuery 的行为是：当 `$.ajax()` 指定了与 `$.ajaxSetup()` 同名的回调时，局部回调会覆盖全局回调，而非追加执行。** 因此全局 `complete` 中的 `hideLoading()` 被跳过，遮罩层永远不会被关闭。

### 对比：为什么其他请求没有此问题

- `fetchUser()`、`fetchMenus()`、`fetchOverview()` 等函数使用 `$.get()`，没有提供 `complete` 回调，全局 `complete` 正常触发 → 遮罩层正常关闭
- `unlockScreen()` 使用 `$.ajax()` 且仅提供了 `success` 回调，没有 `complete` → 全局 `complete` 正常触发
- `saveProfile()` 同时提供了 `success` 和 `complete` → 局部 `complete` 覆盖了全局 `complete` → Bug

## 修复方案

在 `saveProfile()` 的局部 `complete` 回调中显式调用 `AppCommon.hideLoading()`。

### 修改文件

`frontend/src/assets/js/app.js` — `saveProfile()` 函数

### 修复前

```javascript
complete: function () {
    $('#profile-save-btn').prop('disabled', false).text('保存修改');
}
```

### 修复后

```javascript
complete: function () {
    AppCommon.hideLoading();
    $('#profile-save-btn').prop('disabled', false).text('保存修改');
}
```

## 验证方法

1. 登录系统后，点击侧边栏"个人中心"菜单
2. 修改昵称或头像链接
3. 点击"保存修改"
4. 观察遮罩层应短暂出现后自动消失，按钮恢复可用状态，Toast 提示"资料更新成功"

## 防范建议

在后续开发中使用 `$.ajax()` 时，如果需要提供局部 `complete` / `error` / `beforeSend` 回调，务必在回调内补充全局逻辑（如 `hideLoading()`），或改用 `skipGlobalLoading: true` 跳过全局加载状态后自行管理遮罩层生命周期。

也可以考虑将 `$.ajaxSetup` 中的全局回调迁移到 jQuery 全局事件（`$(document).ajaxComplete()`、`$(document).ajaxSend()` 等），这些全局事件不会被局部回调覆盖，更加安全。

---

# 修改密码弹窗点击确认无反应 — 修复指南

## 问题描述

用户点击顶部导航栏"改密"按钮打开修改密码弹窗，输入旧密码、新密码、确认密码后，点击"确认修改"按钮没有任何反馈，弹窗不会关闭也不会出现提示。

## 根因分析

### 现象复现路径

1. 用户点击"改密" → 弹窗正常打开 ✅
2. 输入旧密码（如 `123456`）、新密码（如 `12345678`）、确认密码 → 点击"确认修改"
3. 前端校验发现新密码不满足强度要求（如缺少大写/小写字母）→ 调用 `AppCommon.showToast()` 显示错误
4. **Toast 被弹窗遮挡，用户完全看不到任何提示** ❌
5. 校验提前 return，按钮状态未变化 → 用户感知"无反应"

### 核心原因：Toast z-index 低于 Bootstrap Modal

Bootstrap 4 的层级关系：

| 元素 | z-index |
|------|---------|
| `.modal` | 1050 |
| `.modal-backdrop` | 1040 |
| `.toast-container`（修复前） | 未设置（默认远低于 1050） |

`common.js` 中 `showToast()` 将提示渲染到 `#appToast`，该元素位于 `.toast-container` 内。由于 `.toast-container` 没有显式 z-index，其层叠上下文远低于 `.modal`，导致 Toast 在弹窗打开时被完全遮挡。

与此同时，`submitChangePassword()` 中所有校验失败分支都仅调用 `AppCommon.showToast()` 进行反馈，没有任何行内提示机制。校验失败时按钮状态也不会变化（`disabled` 和文字修改在校验通过之后），用户看不到任何视觉变化。

### 次要问题：AJAX error 回调缺失

`submitChangePassword()` 的 `$.ajax()` 调用未提供局部 `error` 回调。虽然 `$.ajaxSetup` 中有全局 `error` 处理，但当后端返回业务错误（HTTP 200 + code != 0）时走的是 `success` 回调；若后端返回 HTTP 4xx/5xx，全局 `error` 也会用 Toast 提示——同样被弹窗遮挡。

## 修复方案

两层修复，确保弹窗场景下错误反馈始终可见：

### 修复 1：提升 Toast 容器 z-index

**文件**：`frontend/src/assets/css/styles.css`

**修复前**：`.toast-container` 无自定义样式

**修复后**：

```css
.toast-container {
    position: fixed;
    top: 16px;
    right: 16px;
    z-index: 1070;
}
```

将 z-index 设为 1070（高于 Bootstrap modal 的 1050），确保 Toast 在弹窗之上可见。

### 修复 2：弹窗内使用行内错误提示替代 Toast

**文件**：`frontend/src/index.html` + `frontend/src/assets/js/app.js`

在弹窗表单内增加 `<div id="cp-error-msg">` 错误提示区域，校验失败和后端错误直接在弹窗内展示，不依赖 Toast。

**HTML 修改**（`index.html` 修改密码弹窗的 `<div class="modal-body">` 内首行）：

```html
<div id="cp-error-msg" class="alert alert-danger d-none py-2 mb-3" role="alert"></div>
```

**JS 修改**（`app.js`）：

- 新增 `showCpError(msg)` 函数，控制 `#cp-error-msg` 的显示/隐藏
- `submitChangePassword()` 中所有校验失败分支改用 `showCpError()` 替代 `AppCommon.showToast()`
- AJAX `success` 回调中业务错误也改用 `showCpError()`
- 新增 AJAX `error` 回调，HTTP 错误也在弹窗内提示
- `resetChangePasswordForm()` 中增加 `$('#cp-error-msg').addClass('d-none').text('')` 重置错误提示
- 仅在密码修改成功关闭弹窗后使用 `AppCommon.showToast()` 显示成功提示

## 验证方法

1. 登录系统后，点击顶部导航栏"改密"按钮
2. 输入旧密码 `123456`，新密码 `12345678`（不满足强度要求），确认密码 `12345678`
3. 点击"确认修改" → 弹窗内应出现红色错误提示"新密码须包含小写字母"
4. 修正新密码为 `Abc12345`，确认密码 `Abc12345`，点击"确认修改"
5. 弹窗关闭，页面右上角 Toast 提示"密码修改成功，其他设备已下线"

## 防范建议

- 在 Bootstrap Modal 内的表单，优先使用行内错误提示（alert / invalid-feedback）而非全局 Toast，避免层级遮挡问题
- 如需在 Modal 场景使用 Toast，必须确保 `.toast-container` 的 z-index 高于 `.modal`（≥ 1070）
- 对所有涉及 Modal 内 AJAX 操作的场景，均应提供局部 `error` 回调并在 Modal 内展示错误，而非仅依赖全局 `error` 处理

---

# 登录失败遮罩层不关闭且无错误提示 — 修复指南

## 问题描述

用户在登录页面输入错误的用户名或密码后，全局加载遮罩层（`#global-loading`）出现后不会自动关闭，页面被遮挡无法操作，同时登录失败的 Toast 提示也完全不可见。

## 根因分析

### 调用链路

1. 用户点击"登录系统" → 触发 `submitLogin()` 函数
2. `submitLogin()` 内部调用 `$.ajax()` 发起 `POST /api/auth/login` 请求
3. 请求发送前，`$.ajaxSetup` 中的全局 `beforeSend` 回调调用 `showLoading()` → 遮罩层出现 ✅
4. 请求完成后，局部 `complete` 回调仅恢复了按钮状态 → 遮罩层未关闭 ❌
5. `success` 回调中 `handleLoginError()` 调用了 `AppCommon.showToast()` → 但 Toast z-index（1070）低于遮罩层 z-index（9999）→ 提示被遮挡不可见 ❌

### 核心原因：jQuery 回调覆盖机制（与"保存修改"Bug 同源）

在 `common.js` 中，`$.ajaxSetup()` 配置了全局 `complete` 回调：

```javascript
// common.js - 全局配置
$.ajaxSetup({
    beforeSend: function (xhr, settings) {
        if (!settings.skipGlobalLoading) {
            showLoading();  // 显示遮罩层
        }
        // ...
    },
    complete: function (_, statusText) {
        hideLoading();  // 隐藏遮罩层 ← 关键逻辑
        // ...
    }
});
```

在 `login.js` 的 `submitLogin()` 中，`$.ajax()` 提供了局部 `complete` 回调：

```javascript
// login.js - 修复前（有 Bug）
$.ajax({
    // ...
    complete: function () {
        // 仅恢复按钮状态，未调用 hideLoading()
        $('#login-btn').prop('disabled', false).text('登录系统');
    }
});
```

**jQuery 的行为是：当 `$.ajax()` 指定了与 `$.ajaxSetup()` 同名的回调时，局部回调会覆盖全局回调，而非追加执行。** 因此全局 `complete` 中的 `hideLoading()` 被跳过，遮罩层永远不会被关闭。

遮罩层（`#global-loading`）的 z-index 为 9999，而 Toast 容器（`.toast-container`）的 z-index 仅为 1070。遮罩层覆盖在 Toast 之上，导致所有错误提示都被遮挡，用户既看不到遮罩关闭，也看不到任何失败提示。

### 对比：为什么登录成功时未暴露此问题

登录成功时 `success` 回调会在 400ms 后执行 `window.location.href = 'index.html'` 跳转页面，页面跳转本身会销毁当前 DOM，遮罩层随之消失。因此成功场景下遮罩残留不可感知，只有失败场景（停留在登录页）才暴露问题。

## 修复方案

在 `submitLogin()` 的局部 `complete` 回调中显式调用 `AppCommon.hideLoading()`。

### 修改文件

`frontend/src/assets/js/login.js` — `submitLogin()` 函数

### 修复前

```javascript
complete: function () {
    $('#login-btn').prop('disabled', false).text('登录系统');
}
```

### 修复后

```javascript
complete: function () {
    AppCommon.hideLoading();
    $('#login-btn').prop('disabled', false).text('登录系统');
}
```

## 验证方法

1. 打开登录页面，输入错误的用户名或密码
2. 点击"登录系统"
3. 遮罩层应短暂出现后自动消失，Toast 提示"用户名或密码错误"，同时显示剩余可尝试次数
4. 连续失败至账户被锁定后，遮罩层仍应正常关闭，Toast 提示"账户已被锁定"，下方显示解锁倒计时

## 防范建议

- 此问题与此前"保存修改遮罩层不关闭"Bug 根因完全一致，均属 jQuery `$.ajax()` 局部回调覆盖全局回调的同一类问题
- 建议全局排查项目中所有 `$.ajax()` 调用，确认凡是提供了局部 `complete` 回调的，均已在回调内补充 `AppCommon.hideLoading()` 调用
- 更彻底的方案是将 `$.ajaxSetup` 中的全局回调迁移到 jQuery 全局事件（`$(document).ajaxSend()`、`$(document).ajaxComplete()`），这些全局事件不会被局部回调覆盖，可从根源消除此类问题

---

# Docker 容器 admin_backend unhealthy 导致全链路启动失败 — 修复指南

## 问题描述

执行 `docker-compose up --build` 时，`admin_backend` 容器启动后始终处于 unhealthy 状态，依赖 backend 健康检查的 `admin_frontend` 容器因 `dependency failed to start: container admin_backend is unhealthy` 而无法启动。

完整错误信息：

```
✘ Container admin_backend      Error
✘ Container admin_frontend     Error dependency backend failed to start (41.3s)
错误提示："dependency failed to start: container admin_backend is unhealthy"
```

## 根因分析

### 调用链路

1. `docker-compose up` 启动 `db` → `redis` → `backend` → `frontend` 依次启动
2. `db` 和 `redis` 各自的 healthcheck 使用 `mysqladmin ping` 和 `redis-cli ping`，均可正常通过 ✅
3. `backend` 容器的 healthcheck 配置为 `wget -qO- http://localhost:8080/actuator/health | grep -q '"status":"UP"'`
4. backend 运行时镜像基于 `eclipse-temurin:17-jre`，该精简 JRE 镜像**未预装 `wget` 或 `curl`** ❌
5. healthcheck 命令执行时 `wget: not found` → 每次检查均失败 → retries 耗尽后标记为 unhealthy
6. `frontend` 配置 `depends_on: backend: condition: service_healthy` → backend 始终不健康 → frontend 无法启动

### 核心原因：healthcheck 使用了容器内不存在的命令

| 组件 | 基础镜像 | healthcheck 命令 | 镜像内是否可用 |
|------|----------|-----------------|---------------|
| db | mysql:8.0 | `mysqladmin ping` | ✅ MySQL 自带 |
| redis | redis:7-alpine | `redis-cli ping` | ✅ Redis 自带 |
| backend | eclipse-temurin:17-jre | `wget -qO-` | ❌ JRE 镜像无 wget/curl |

### 次要问题：start_period 过短

原配置 `start_period: 20s`，而 Spring Boot 应用在容器环境中从 JVM 启动到 Bean 初始化、数据库连接池建立、Redis 连接建立完成通常需要 30-50 秒。20 秒的启动宽限期意味着应用尚未就绪时 healthcheck 已开始计入失败次数，即使命令存在也可能导致启动初期多次失败消耗 retries。

## 修复方案

### 修复 1：Dockerfile 中安装 curl

**文件**：`backend/Dockerfile`

在运行时阶段安装 `curl`（选择 curl 而非 wget，因为 curl 功能更全面且 Alpine/Debian 生态更常用），同时清理 apt 缓存以控制镜像体积。

**修复前**：

```dockerfile
FROM eclipse-temurin:17-jre
WORKDIR /app
ENV TZ=Asia/Shanghai
COPY --from=build /app/target/backend-0.0.1-SNAPSHOT.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-Duser.timezone=Asia/Shanghai", "-jar", "/app/app.jar"]
```

**修复后**：

```dockerfile
FROM eclipse-temurin:17-jre
WORKDIR /app
ENV TZ=Asia/Shanghai
RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*
COPY --from=build /app/target/backend-0.0.1-SNAPSHOT.jar /app/app.jar
EXPOSE 8080
ENTRYPOINT ["java", "-Duser.timezone=Asia/Shanghai", "-jar", "/app/app.jar"]
```

### 修复 2：docker-compose.yml 中改用 curl 并增大 start_period

**文件**：`docker-compose.yml`

**修复前**：

```yaml
healthcheck:
  test: ["CMD-SHELL", "wget -qO- http://localhost:8080/actuator/health | grep -q '\"status\":\"UP\"'"]
  interval: 10s
  timeout: 5s
  retries: 20
  start_period: 20s
```

**修复后**：

```yaml
healthcheck:
  test: ["CMD-SHELL", "curl -sf http://localhost:8080/actuator/health | grep -q '\"status\":\"UP\"'"]
  interval: 10s
  timeout: 5s
  retries: 20
  start_period: 60s
```

变更说明：
- `wget -qO-` → `curl -sf`：`-s` 静默模式，`-f` 在 HTTP 错误时返回非零退出码，更可靠
- `start_period: 20s` → `60s`：给予 Spring Boot 应用充足的启动时间，避免启动初期误判

### 修复 3：补充 MyBatis-Plus 逻辑删除全局配置

在本次用户管理模块开发中，`SysUser` 实体新增了 `@TableLogic` 注解用于逻辑删除。为确保 MyBatis-Plus 全局行为一致，在 `application.yml` 中补充了逻辑删除的全局配置。

**文件**：`backend/src/main/resources/application.yml`

**修复前**：

```yaml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
```

**修复后**：

```yaml
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0
```

## 验证方法

1. 确保已有 `.env` 文件（可从 `.env.example` 复制）
2. 执行 `docker-compose down -v` 清除旧容器和数据卷
3. 执行 `docker-compose up --build -d`
4. 观察容器状态：`docker-compose ps`，四个容器应均为 `running (healthy)`
5. 查看后端日志：`docker-compose logs backend`，确认无启动异常
6. 访问 `http://localhost:3000` 应正常进入登录页面

## 防范建议

- 为基于精简镜像（JRE/Alpine 等）的容器配置 healthcheck 时，务必确认镜像内是否包含 healthcheck 所需的工具（curl/wget/netcat 等），否则应在 Dockerfile 中安装
- Spring Boot 应用的 `start_period` 应根据实际启动耗时合理设置，建议不低于 60 秒，尤其在容器环境下首次启动涉及依赖连接池初始化
- 健康检查优先使用 `curl -sf` 而非 `wget`，curl 的 `-f` 参数在 HTTP 4xx/5xx 时返回非零退出码，语义更准确
- 新增 MyBatis-Plus 的 `@TableLogic` 字段时，建议同时在 `application.yml` 中声明 `logic-delete-field`/`logic-delete-value`/`logic-not-delete-value`，确保全局与注解行为一致

---

# 新增顶级菜单目录后无法添加子菜单  修复指南

## 问题描述

用户在"菜单权限"页面点击"新增顶级菜单"按钮，创建了一个新的顶级菜单目录（例如名称为"业务管理"，路径为 #）后，新菜单行的操作按钮中没有出现绿色的"+"按钮（添加子菜单按钮），无法为该目录继续添加下级菜单。只有那些原本就已有子菜单的老菜单（如"系统管理"）才会显示添加子菜单按钮。

## 根因分析

### 渲染逻辑溯源

在 pp.js 的 uildManageTreeHtml() 函数中，每行菜单的操作按钮区域是通过如下代码渲染的：

``javascript
// app.js - buildManageTreeHtml() 修复前
var hasChildren = Array.isArray(node.children) && node.children.length > 0;

// ...
'<span class="menu-manage-actions ml-auto">';
if (hasChildren) {
    html += '<button class="btn btn-sm btn-outline-success btn-menu-add-child ...">...</button>';
}
html += '<button class="btn btn-sm btn-outline-info btn-menu-edit ...">...</button>' + ...
``

### 核心原因：按钮可见性与"已有子菜单"绑定

"添加子菜单"按钮的显示被包裹在 if (hasChildren) 条件内，即**只有当前菜单已经拥有子菜单时，才允许继续添加子菜单**。

这个条件判断混淆了两个不同的语义：
- **是否需要折叠箭头**（hasChildren  显示 chevron 或 placeholder）：这个判断是正确的，没有子菜单就不需要展开/折叠箭头
- **是否允许添加子菜单**：任何菜单（无论当前是否有子菜单）都应该允许添加子菜单，因为菜单目录本身就是用来容纳下级菜单的容器

新创建的顶级菜单目录初始状态下 children 为空数组  hasChildren = false  添加子菜单按钮被条件隐藏  用户无法为新目录添加下级菜单。

### 为什么老菜单没有问题

已有的"系统管理"等菜单在初始化种子数据中就带有子菜单（用户信息、菜单权限、用户管理），因此 hasChildren = true，按钮始终可见，功能正常。

## 修复方案

移除"添加子菜单"按钮的 if (hasChildren) 条件包裹，让该按钮在所有菜单行中始终显示。

### 修改文件

rontend/src/assets/js/app.js  uildManageTreeHtml() 函数

### 修复前

``javascript
'<span class="menu-manage-actions ml-auto">';
if (hasChildren) {
    html += '<button class="btn btn-sm btn-outline-success btn-menu-add-child mr-1" title="添加子菜单" data-id="' + node.id + '" data-title="' + escapeHtml(node.title || '') + '"><i class="fas fa-plus"></i></button>';
}
html += '<button class="btn btn-sm btn-outline-info btn-menu-edit mr-1" title="编辑"><i class="fas fa-edit"></i></button>' + ...
``

### 修复后

``javascript
'<span class="menu-manage-actions ml-auto">' +
'<button class="btn btn-sm btn-outline-success btn-menu-add-child mr-1" title="添加子菜单" data-id="' + node.id + '" data-title="' + escapeHtml(node.title || '') + '"><i class="fas fa-plus"></i></button>' +
'<button class="btn btn-sm btn-outline-info btn-menu-edit mr-1" title="编辑"><i class="fas fa-edit"></i></button>' + ...
``

注意：hasChildren 变量仍需保留，因为折叠箭头的显示逻辑以及子菜单的递归渲染仍然需要依赖该判断。

## 验证方法

1. 登录系统后，点击侧边栏"系统管理  菜单权限"进入菜单管理页面
2. 点击顶部"新增顶级菜单"按钮，填入：
   - 菜单名称：业务管理
   - 菜单路径：#
   - 图标：as fa-briefcase
   - 权限码：iz:root
   - 其余保持默认
3. 点击"确认保存"，观察菜单列表中新增的"业务管理"行
4. **修复前**：该行操作区只有编辑、上移、下移、显示/隐藏、删除 5 个按钮，缺少绿色 + 按钮
5. **修复后**：该行操作区最左侧应显示绿色"+"按钮（添加子菜单），点击可正常打开新增子菜单弹窗
6. 通过该按钮为"业务管理"添加一个子菜单（如"订单列表"），保存后应立即显示在"业务管理"下方，侧边栏导航同步刷新

## 防范建议

- **UI 控件可见性应依据"权限/能力"而非"当前状态"**：按钮是否显示应基于用户是否拥有该操作的权限，以及业务逻辑上该操作是否被允许（例如"删除"按钮因存在子菜单而禁用是合理的），而非基于当前数据是否为空（例如"添加子菜单"不应因当前没有子菜单就被隐藏）
- **首次开发时建立最小复现验证**：对"新增  继续操作"类的流程，务必用全新创建的、空状态的资源走一遍完整操作链路，不要只验证已有数据的老资源
- **区分"展示状态"和"操作能力"的判断变量**：可考虑使用独立变量如 canAddChild（始终为 true，或基于权限码判断）和 hasChildren（用于渲染折叠 UI），避免共用一个判断条件导致非预期行为

---

# 新增顶级菜单目录后无法添加子菜单 — 修复指南

## 问题描述

用户在"菜单权限"页面点击"新增顶级菜单"按钮，创建了一个新的顶级菜单目录（例如名称为"业务管理"，路径为 `#`）后，新菜单行的操作按钮中没有出现绿色的"+"按钮（添加子菜单按钮），无法为该目录继续添加下级菜单。只有那些原本就已有子菜单的老菜单（如"系统管理"）才会显示添加子菜单按钮。

## 根因分析

### 渲染逻辑溯源

在 `app.js` 的 `buildManageTreeHtml()` 函数中，每行菜单的操作按钮区域是通过如下代码渲染的：

```javascript
// app.js - buildManageTreeHtml() 修复前
var hasChildren = Array.isArray(node.children) && node.children.length > 0;

// ...
'<span class="menu-manage-actions ml-auto">';
if (hasChildren) {
    html += '<button class="btn btn-sm btn-outline-success btn-menu-add-child ...">...</button>';
}
html += '<button class="btn btn-sm btn-outline-info btn-menu-edit ...">...</button>' + ...
```

### 核心原因：按钮可见性与"已有子菜单"绑定

"添加子菜单"按钮的显示被包裹在 `if (hasChildren)` 条件内，即**只有当前菜单已经拥有子菜单时，才允许继续添加子菜单**。

这个条件判断混淆了两个不同的语义：
- **是否需要折叠箭头**（`hasChildren` → 显示 chevron 或 placeholder）：这个判断是正确的，没有子菜单就不需要展开/折叠箭头
- **是否允许添加子菜单**：任何菜单（无论当前是否有子菜单）都应该允许添加子菜单，因为菜单目录本身就是用来容纳下级菜单的容器

新创建的顶级菜单目录初始状态下 `children` 为空数组 → `hasChildren = false` → 添加子菜单按钮被条件隐藏 → 用户无法为新目录添加下级菜单。

### 为什么老菜单没有问题

已有的"系统管理"等菜单在初始化种子数据中就带有子菜单（用户信息、菜单权限、用户管理），因此 `hasChildren = true`，按钮始终可见，功能正常。

## 修复方案

移除"添加子菜单"按钮的 `if (hasChildren)` 条件包裹，让该按钮在所有菜单行中始终显示。

### 修改文件

`frontend/src/assets/js/app.js` — `buildManageTreeHtml()` 函数

### 修复前

```javascript
'<span class="menu-manage-actions ml-auto">';
if (hasChildren) {
    html += '<button class="btn btn-sm btn-outline-success btn-menu-add-child mr-1" title="添加子菜单" data-id="' + node.id + '" data-title="' + escapeHtml(node.title || '') + '"><i class="fas fa-plus"></i></button>';
}
html += '<button class="btn btn-sm btn-outline-info btn-menu-edit mr-1" title="编辑"><i class="fas fa-edit"></i></button>' + ...
```

### 修复后

```javascript
'<span class="menu-manage-actions ml-auto">' +
'<button class="btn btn-sm btn-outline-success btn-menu-add-child mr-1" title="添加子菜单" data-id="' + node.id + '" data-title="' + escapeHtml(node.title || '') + '"><i class="fas fa-plus"></i></button>' +
'<button class="btn btn-sm btn-outline-info btn-menu-edit mr-1" title="编辑"><i class="fas fa-edit"></i></button>' + ...
```

注意：`hasChildren` 变量仍需保留，因为折叠箭头的显示逻辑以及子菜单的递归渲染仍然需要依赖该判断。

## 验证方法

1. 登录系统后，点击侧边栏"系统管理 → 菜单权限"进入菜单管理页面
2. 点击顶部"新增顶级菜单"按钮，填入：
   - 菜单名称：`业务管理`
   - 菜单路径：`#`
   - 图标：`fas fa-briefcase`
   - 权限码：`biz:root`
   - 其余保持默认
3. 点击"确认保存"，观察菜单列表中新增的"业务管理"行
4. **修复前**：该行操作区只有编辑、上移、下移、显示/隐藏、删除 5 个按钮，缺少绿色 + 按钮
5. **修复后**：该行操作区最左侧应显示绿色"+"按钮（添加子菜单），点击可正常打开新增子菜单弹窗
6. 通过该按钮为"业务管理"添加一个子菜单（如"订单列表"），保存后应立即显示在"业务管理"下方，侧边栏导航同步刷新

## 防范建议

- **UI 控件可见性应依据"权限/能力"而非"当前状态"**：按钮是否显示应基于用户是否拥有该操作的权限，以及业务逻辑上该操作是否被允许（例如"删除"按钮因存在子菜单而禁用是合理的），而非基于当前数据是否为空（例如"添加子菜单"不应因当前没有子菜单就被隐藏）
- **首次开发时建立最小复现验证**：对"新增 → 继续操作"类的流程，务必用全新创建的、空状态的资源走一遍完整操作链路，不要只验证已有数据的老资源
- **区分"展示状态"和"操作能力"的判断变量**：可考虑使用独立变量如 `canAddChild`（始终为 true，或基于权限码判断）和 `hasChildren`（用于渲染折叠 UI），避免共用一个判断条件导致非预期行为

